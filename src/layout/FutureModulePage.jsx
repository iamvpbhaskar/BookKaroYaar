import { Box, Typography } from '@mui/material'
export function FutureModulePage({ title }) { return <Box className="future-page"><Typography className="dashboard-eyebrow">ON THE WAY</Typography><Typography variant="h1">{title}</Typography><Typography>This space will plug into your shared plans when its dedicated module is ready.</Typography></Box> }
